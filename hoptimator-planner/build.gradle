apply plugin: 'java'
apply plugin: 'application' // to enable running demo

application {
  mainClass ='com.linkedin.hoptimator.planner.HoptimatorPlannerDemo'
}
 
dependencies {
  implementation project(':hoptimator-catalog')

  implementation libs.calciteAvatica
  implementation libs.calciteCore
}

tasks.withType(JavaCompile) {
  options.compilerArgs << '-Xlint:deprecation'
  options.compilerArgs << '-Xlint:unchecked'
}
