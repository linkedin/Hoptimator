apiVersion: hoptimator.linkedin.com/v1alpha1
kind: KafkaTopic
metadata:
  name: hoptimator-{{pipeline.name}}-{{name}}-kafka-topic
  namespace: {{pipeline.namespace}}
spec:
  name: hoptimator-{{pipeline.name}}-{{name}}
