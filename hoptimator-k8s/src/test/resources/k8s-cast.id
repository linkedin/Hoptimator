!set outputformat mysql
!use k8s

create or replace table ads."cast_target" ("member_id" bigint, "name" varchar);
(0 rows modified)

!update

create or replace materialized view ads."cast_target$bad" ("member_id") as select member_urn AS "member_id" from profile.members;
Incompatible types for sink column 'member_id': query produces VARCHAR but sink expects BIGINT (no safe conversion is available under castMode=strict).
!error

drop table ads."cast_target";
(0 rows modified)

!update
