apiVersion: hoptimator.linkedin.com/v1alpha1
kind: KafkaTopic
metadata:
  name: {{name}}
  namespace: {{namespace}}
spec:
  topicName: {{name}}
  numPartitions: {{numPartitions}}
  clientOverrides:
    {{clientOverrides}}
